# Server port
PORT=5000

# MongoDB Atlas connection string (replace with your own)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/scm_database?retryWrites=true&w=majority

# Secret key used to sign JWT tokens (use any long random string)
JWT_SECRET=replace_this_with_a_long_random_secret_string
